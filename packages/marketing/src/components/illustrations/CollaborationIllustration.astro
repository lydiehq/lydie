<div
  class="w-full aspect-[4/3] bg-gradient-to-br from-blue-50 via-indigo-50 to-purple-50 p-8 flex flex-col"
>
  <!-- Document container -->
  <div
    class="flex-1 bg-white rounded-lg shadow-sm border border-gray-200 p-6 relative overflow-hidden"
  >
    <!-- Document content with actual text -->
    <div class="space-y-4 text-gray-700">
      <!-- Title -->
      <div class="h-7 flex items-center">
        <span class="text-lg font-semibold text-gray-900">Project Roadmap</span>
        <div class="ml-2 w-2 h-2 bg-blue-500 rounded-full animate-pulse"></div>
      </div>

      <!-- Paragraph lines with some text -->
      <div class="space-y-2 text-sm">
        <div class="flex items-center gap-2">
          <span class="text-gray-600">Q1: Launch MVP with core features</span>
        </div>
        <div class="flex items-center gap-2">
          <span class="text-gray-600">Q2: Add integrations and API</span>
        </div>
        <div class="flex items-center gap-2">
          <span class="text-gray-400">Q3: Scale infrastructure and</span>
          <span class="inline-block w-2 h-4 bg-indigo-600 animate-pulse"></span>
        </div>
      </div>

      <!-- More content -->
      <div class="space-y-2 mt-6 text-sm text-gray-600">
        <div>Key milestones:</div>
        <div class="pl-4 space-y-1">
          <div>• Beta testing phase</div>
          <div>• Public launch</div>
        </div>
      </div>
    </div>

    <!-- First cursor (left side, Sarah) -->
    <div class="absolute top-20 left-12 flex items-start gap-2 z-10">
      <!-- Cursor line -->
      <div class="w-0.5 h-8 bg-blue-600"></div>
      <!-- Cursor pointer -->
      <div class="flex flex-col items-center -mt-1">
        <div
          class="w-3 h-3 bg-blue-600 rounded-sm transform rotate-45 shadow-md"
        >
        </div>
        <div class="w-2 h-2 bg-blue-600 rounded-full -mt-1.5"></div>
      </div>
      <!-- Name badge -->
      <div
        class="bg-blue-600 text-white text-xs px-2.5 py-1 rounded-md shadow-lg font-medium whitespace-nowrap"
      >
        Sarah
      </div>
    </div>

    <!-- Second cursor (right side, Alex - typing) -->
    <div class="absolute top-36 right-16 flex items-start gap-2 z-10">
      <!-- Cursor line -->
      <div class="w-0.5 h-8 bg-indigo-600"></div>
      <!-- Cursor pointer -->
      <div class="flex flex-col items-center -mt-1">
        <div
          class="w-3 h-3 bg-indigo-600 rounded-sm transform rotate-45 shadow-md"
        >
        </div>
        <div class="w-2 h-2 bg-indigo-600 rounded-full -mt-1.5"></div>
      </div>
      <!-- Name badge -->
      <div
        class="bg-indigo-600 text-white text-xs px-2.5 py-1 rounded-md shadow-lg font-medium whitespace-nowrap"
      >
        Alex
      </div>
      <!-- Typing indicator -->
      <div class="flex gap-1 ml-1 mt-1">
        <div class="w-1.5 h-1.5 bg-indigo-400 rounded-full animate-pulse"></div>
        <div
          class="w-1.5 h-1.5 bg-indigo-400 rounded-full animate-pulse"
          style="animation-delay: 0.15s"
        >
        </div>
        <div
          class="w-1.5 h-1.5 bg-indigo-400 rounded-full animate-pulse"
          style="animation-delay: 0.3s"
        >
        </div>
      </div>
    </div>

    <!-- Live edit indicator -->
    <div
      class="absolute top-4 right-4 flex items-center gap-2 bg-green-50 border border-green-200 rounded-full px-3 py-1.5 shadow-sm"
    >
      <div class="w-2 h-2 bg-green-500 rounded-full animate-pulse"></div>
      <span class="text-xs font-medium text-green-700">2 people editing</span>
    </div>
  </div>
</div>
